## 2026-08-18｜Agent 角色定义优化
决策：角色由单一“决策 Agent”调整为“项目架构师 + 决策 Agent”，统一负责设计文档、架构决策、执行提示词和实现复核。
原则：第一版继续以核心链路上线跑通为优先，非核心边界允许简化或延期。

## 2026-08-18｜归因分析数据对象设计冻结
决策：冻结 RouteResult、AttributionTarget、Action、QueryExecutionResult、Observation、Calculation、Evidence、AttributionState、AttributionReport 九类核心对象；现有 DataAgentState 保持问数 Graph 内部职责，不扩展为归因状态。
原则：Observation 同时保留原始查询结果和归一化结果，Calculator 只消费归一化事实；Planner 不直接生成 SQL；所有归因对象第一版均不持久化。

## 2026-08-18｜统一分析 API 与 SSE 契约冻结
决策：第一版继续使用单一 POST /api/query，通过 mode=auto|query|attribution 控制分析模式；普通问数与经营归因统一使用 SSE，事件通过 JSON type 区分，冻结 route/stage/action_start/query_result/calculation/report/error/done 八类事件。
原则：接口层不暴露完整内部 State；查询事实由 query_result 提供，确定性数值由 calculation 提供，归因结论由 report 提供，所有已建立的流最终以 done 明确 completed/partial/failed 状态。

## 2026-08-18｜模块级 SPEC 前增加只读实施勘察
决策：数据对象与 API 设计冻结后，不提前创建代码框架；先由执行 Agent 对现有代码进行只读实施勘察，再由架构 Agent结合勘察结果生成模块级实施 SPEC。
原则：文件划分、模块职责和依赖方向属于 SPEC 决策范围，避免先实现后补设计；勘察阶段不得修改代码。

## 2026-08-18｜模块级实施 SPEC 冻结
决策：第一版按 7 个 Stage 增量实施；公共分析对象统一放入 `app/models/analysis.py`，现有问数 Graph 仅做结构化结果与稳定 stage_code 的最小改造；Attribution Graph 独立运行并通过请求级 QueryService 复用问数能力。
原则：`analysis_id` 复用现有 `req_id`；归因子查询使用内部 `result_contract` 保证结果可归一化；不新增无必要 Provider 层和基础设施；每个 Stage 完成后停止并复核，普通问数基线必须持续可用。


## 2026-08-18｜Stage 1 验收通过并进入 Stage 2
决策：接受提交 `ac56a6a2aec42130c39a0b3003fce66d939cebf8`，Stage 1 销售经营口径切换、pytest 基础及五问回归验收通过，允许进入 Stage 2 QueryService 结构化执行边界；Stage 1 新增 GROUP BY 规则继续保留。
原则：`result_contract` 只负责结构和别名约束，不作为业务聚合粒度的确定性兜底；重复业务维度拆行保留为后续归一化阶段复核项。`.workbuddy` 记忆文件、ES 索引硬编码及 meta2 重建限制均不阻塞当前阶段，不扩大 Stage 2 范围处理。


## 2026-08-18｜Stage 2 主体验收通过，契约小修后进入 Stage 3
决策：提交 `e30a07438dd5e79581465c8fbc10ef192dc4204f` 的 QueryService 结构化执行边界整体符合冻结 SPEC；保留现有实现。但发现 legacy `/api/query` 泄露内部 `query_result`、QueryTable 数据库值预归一化时机错误、QueryExecutionResult empty 状态校验不完整三项 Stage 2 范围内问题，修复并通过回归后方可进入 Stage 3。
原则：不扩大修复范围，不返工现有 QueryService/result_contract/stage_code 设计；failed 端到端注入验证、普通问数自由列别名波动继续按既有计划延期处理。

## 2026-08-18｜Stage 2 最终验收通过并进入 Stage 3
决策：接受提交 `5439a1cb939f267a66db53a7dc128c34c0e21d2b`，三项契约修复均闭合，Stage 2 QueryService 结构化执行边界正式验收通过；允许进入 Stage 3，实施统一 AnalysisService、新 SSE 契约及普通问数前端迁移。
原则：数据库值归一化固定在 Repository 读取边界，QueryTable 保持纯结构校验；`result_contract` 单次 LLM 波动不阻塞当前阶段，由后续 Normalizer 严格契约校验兜底；Stage 3 不提前实现任何归因确定性核心或 Attribution Graph。

## 2026-08-18｜Stage 3 验收通过并进入 Stage 4
决策：接受提交 `d67b3db1634cea77865428e663a501ad53be3e31`，统一 AnalysisService、新 SSE 契约、Intent Router 及普通问数前端迁移符合冻结 SPEC，Stage 3 正式验收通过；允许进入 Stage 4 归因确定性核心。
原则：Stage 4 只实现 AttributionState、Action Router、Normalizer、Calculator、Evidence Builder 及相关数据对象和纯函数停止条件；不得提前实现 Target Parser、Planner、Attribution Graph、Report Generator 或归因前端。浏览器人工 UI 验证及 legacy `QueryService.search()` 清理均不阻塞本阶段。

## 2026-08-18｜Stage 4 主体验收通过，finish 契约小修后进入 Stage 5
决策：提交 `773ac5f8b8c28aa59d85e2fe6d330ba2748c2483` 的 AttributionState、Action Router、Normalizer、Calculator、Evidence Builder 及停止条件主体符合冻结 SPEC；保留现有实现。但正常 `finish_analysis` 当前仅判断成功总体 Observation，未落实冻结设计要求的“总体指标比较 Evidence”，修复并通过回归后方可进入 Stage 5。
原则：修复仅收紧 `compare_period Action → success Observation → Evidence` 的 completed 前置条件，不扩大 Stage 4 范围；`analyze_unit_price` 不得被视为总体比较。其余 6 次查询上限、连续两次 empty/failed、partial/failed、Decimal 计算及归一化规则继续保持。

## 2026-08-18｜Stage 4 最终验收通过并进入 Stage 5
决策：接受提交 `880043ece5522a6815337bd9453e3a29db4134f0`，正常 finish 已收紧为 `compare_period Action → success Observation → Evidence` 完整证据链，`analyze_unit_price` 不再可能被误判为总体比较；Stage 4 全部验收完成，允许进入 Stage 5 Target Parser、Planner、Attribution Graph、Report Generator 及 attribution SSE 后端集成。
原则：Stage 5 补齐 `AttributionReport` 正式对象并替换 State 中临时 report 类型；Planner 只产生单个受控 Action，所有数值继续由确定性 Calculator 生成，Report 仅基于 Evidence；查询 Action 最多 6 次，两个冻结归因场景后端事件流稳定前不得进入 Stage 6。

## 2026-08-18｜Stage 5 主体验收通过，契约小修后进入 Stage 6
决策：提交 `5f3b391e3193ba709a8ccd7653eced6a9943a76a` 的 Target Parser、Planner、Attribution Graph、Report Generator 与 attribution SSE 主链路整体符合冻结设计，两个冻结归因场景已跑通；接受手写 async 主循环替代 LangGraph 作为第一版实施例外，不要求重写。但 Target Parser 硬拒绝/显式跨年期间、Planner retry 总次数、action_start 查询计数、Calculation ID 唯一性、Report LLM 契约与数值边界、REPORT_GENERATION_FAILED 六组问题需在 Stage 5 内修复并回归后，方可进入 Stage 6。
原则：本轮仅做 Stage 5 契约收口，不扩大到前端或无关重构；继续保持查询最多 6 次、连续两次 empty/failed 强停、所有关键数值由 Calculator 确定性生成、Report 基于 Evidence 且 LLM 失败可确定性降级。category/product SQL 别名波动继续作为既有技术债，不阻塞本阶段。

## 2026-08-19｜Stage 5 最终验收通过并进入 Stage 6
决策：接受提交 `ce22984c563b77448965e347b694727c66e87251`。Stage 5 复核发现的 Target Parser 硬拒绝与显式比较期间、Planner retry budget、action_start 查询计数、Calculation ID 唯一性、Report LLM 契约与数值守卫、REPORT_GENERATION_FAILED 六组问题均已闭合；Stage 5 正式验收通过，允许进入 Stage 6 归因分析工作台。
原则：继续接受手写 async Attribution Graph 作为 V1 实施方案；Report 数值守卫偏保守、category/product SQL alias 波动及 Report LLM 语言组织成功率均作为非阻塞技术债保留。Stage 6 只完成单页归因展示与两个冻结场景演示，不修改后端归因逻辑，不引入图表库、Pinia、Router、WebSocket 或其它超范围基础设施。

## 2026-08-19｜Stage 6 主体通过，前端小修后再验收
决策：提交 `02e5fca98f0ab35ddba4adf1cf4a5e7040402340` 的归因分析工作台主体符合冻结设计；但报告复制换行错误、复制文本未保持冻结七段结构、Evidence 多 Observation 部分缺失时追溯提示不完整三项问题需在 Stage 6 内修复，且两个冻结场景完成真实浏览器演示后，方可进入 Stage 7。
原则：本轮只做前端 Stage 6 契约收口，不修改后端归因逻辑、不新增依赖或基础设施；现有 `useAnalysisStream.js` 已满足 done/连接异常契约，无需为形式上的文件变更重复修改。

## 2026-08-19｜Stage 6 修复验收通过，待真实页面验收
决策：接受提交 `3ba936b05dbd296a3a11117156eb7fcb1c3f7619`，报告复制七段结构及多 Observation 独立追溯问题均已闭合，Stage 6 前端代码复核通过；由于当前环境无法连接真实 MySQL/Qdrant/ES/LLM，两个冻结归因场景尚未完成真实浏览器演示，因此暂不授权进入 Stage 7。
原则：本阶段不再追加代码修改；真实页面验收属于冻结 Stage 6 完成条件，不作为技术债延期。待可联网环境完成两个归因场景及普通问数真实浏览器回归后，再进行 Stage 6 最终验收。


## 2026-08-19｜Stage 6 最终验收通过并进入 Stage 7
决策：接受基线 `3ba936b05dbd296a3a11117156eb7fcb1c3f7619` 及 `docs/stage6_browser_acceptance.md` 的真实浏览器验收结果，两个冻结归因场景与普通问数链路均已完成真实页面验证，Stage 6 正式验收通过，允许进入 Stage 7。验收中发现的归因后端偶发无有效拆解不回退 Stage 6，但作为 Stage 7 最终验收阻断缺陷处理；两个冻结归因场景分别连续 3 次稳定复算前不得完成第一版最终验收。
原则：优先复现并确认 `result_contract → SQL → QueryExecutionResult → Normalizer` 边界是否为波动根因；若确认，允许在单次 QueryService 执行内部增加最多一次契约失败 SQL 修复，但不得放宽 Normalizer、连续两次 empty/failed 强停、6 次查询上限或硬编码演示 SQL/Planner 路线。无头 Chrome 长 SSE 挂起属于测试脚手架问题，不阻塞产品验收。

## 2026-08-19｜Stage 7 主体通过，契约边界小修后最终冻结
决策：提交 `f1d4461` 的 `result_contract` 硬执行契约 + 单次受控 SQL 修复总体方案接受，根因定位及两个冻结场景稳定复算结果有效；但当前存在 `QueryService → attribution.normalizer` 反向依赖、硬契约校验与 Normalizer 在 period/NULL 语义上不完全一致、生产 `_default_contract_repair` 路径尚未被真实触发验证三项问题，修复并完成受控 repair 验收后方可正式冻结第一版。
原则：仅在 Stage 7 内做契约边界收口，不改变一次内部修复、6 次查询上限、连续两次 empty/failed 强停、Normalizer 严格归一化及现有 Planner/前端设计；SQL 只读硬防护等既有非本阶段问题继续作为后续技术债，不扩大本轮范围。











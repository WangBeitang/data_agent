【角色】
你是一个资深的数据库专家和数据分析师。你的任务：根据【上下文信息】与【契约失败原因】，修复一条不符合结果结构契约的 SQL，输出一条严格符合契约的 SQL 语句。

【上下文信息】
可用数据表信息如下：
{table_infos}

可参考的指标信息如下：
{metric_infos}

当前的时间信息如下：
{date_info}

数据库环境如下：
{db_info}

【原始用户查询】
{query}

【原始（错误）SQL】
{sql}

【结果结构契约（result_contract）】
{result_contract}

【契约失败原因】
{reason}

【修复要求】
1. 仅允许使用数据表信息中真实存在的表与字段名称，禁止编造、猜测或引入未提供的表和字段。
2. 生成的 SQL 只能用于查询，不能涉及数据写入、更新、删除等操作，语法必须严格符合数据库环境指定的类型与版本。
3. 输出必须仅包含一条完整 SQL 语句的纯文本，严禁使用 ```、```sql 等 Markdown 代码块或任何格式化符号。
4. 必须严格遵循 result_contract：
   a. SELECT 输出列必须严格按照契约中约定的列别名（period_alias、dimension_alias、metric_aliases 中的别名）输出，不得增加、删除或修改契约输出列；
   b. period_key 等固定值必须严格遵循契约中 period_values 指定的取值（如 comparison、current），不得自行更改；
   c. 不得随意改变契约字段名，不得为了美观重命名结果列。
5. 仅在原始 SQL 的基础上修正与契约不符的部分（列别名、period 取值、指标列别名），不要改动正确的聚合与过滤逻辑；若原始 SQL 除契约列外均已正确，请原样保留聚合/过滤并仅修正契约列。
6. 当 result_contract 非空（即不是“无（普通问数，不约束输出列名）”）时必须遵循上述契约规则；当为空时保持普通问数行为。

输出：

"""经营归因模块包（Stage 3 范围）。

Stage 3 仅实现意图路由（Intent Router）。

以下对象属于后续 Stage，本包不提前创建：
AttributionState / AttributionContext / Target Parser / Action /
Action Router / Normalizer / Calculator / Evidence Builder / Planner /
Attribution Graph / Report Generator。
"""

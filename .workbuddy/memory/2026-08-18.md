# 2026-08-18 工作日志

## 工作日志纪律约定
- 用户明确要求：本工作空间每次执行任务后必须记录工作日志。
- 已创建 `.workbuddy/memory/` 目录，固化约定至 `MEMORY.md`。
- 约定内容：每次任务后追加写入当日 `YYYY-MM-DD.md`，append-only；当天文件不存在则先创建。

## 归因改造实施前代码勘察（只读，未改任何文件）
- 冻结文档已齐：docs/ 下概要设计总纲、数据对象设计、API接口设计、决策文档。
- 勘察结论要点（供后续模块级 SPEC 使用）：
  - 问数链路：main.py → query_router(POST /api/query) → QueryService.search() → graph.astream(stream_mode="custom") 透传 {"stage"}/{“result”}；execute_sql 节点发 {"result": list[dict]}，无 columns 结构；state 只有 sql/error，结果不进 state。
  - langgraph==1.0.7、Python 3.12、uv；无 pytest 测试套件；graph_test.py 与 app/api/test/、conf/test_config.py、prompts/plan_sql.prompt 均为遗留物，未被引用。
  - 前端 App.vue 单文件：fetch+getReader 解析 SSE，按 data.stage / data.error / Array.isArray(data.result) 分发，无图表依赖。
  - 归因新增推荐放 app/attribution/（state/schemas/intent_router/planner/action_router/provider+normalizer/calculator/evidence_builder/report_generator/graph）+ services/analysis_service.py；QueryService 增结构化 execute()；execute_sql 节点需最小调整（结果写回 state + 提供列名）。
  - 设计缺口：result 字段淘汰需前后端同阶段迁移；空结果列名无法从 list[dict] 推断；现有 stage 事件无成功/失败终态；error 直接透传 str(e) 违反新契约安全要求；analysis_id 与 req_id 关系未冻结。
- 报告以对话形式输出（用户禁止新增文件），未写盘。

## Stage 1：销售经营口径切换 + 测试基础（完成）
- 目标：普通问数从电商口径切换为制造业销售经营口径，建立 pytest 基础；只做 SPEC Stage 1，未进入 Stage 2。
- 修改文件：
  - `data-agent/conf/meta_config.yaml`：指标从 GMV/AOV 改为四个核心指标（销售额 SUM(order_amount)、销售数量 SUM(order_quantity)、销售订单数 COUNT(DISTINCT order_id)、平均单件销售额 SUM(order_amount)/SUM(order_quantity)，后注明"仅表示销售额与销售数量的比值"）；表/字段文案改制造业口径（产品/产品类别/客户等级/销售区域/销售额等）；物理表名、字段名、sync 标志均不变。
  - `data-agent/prompts/extend_keywords_for_metric_recall.prompt`：去掉 GMV/DAU 电商示例，改为销售额/销售数量等制造业口径示例。
  - `data-agent/prompts/generate_sql.prompt`：新增规则 7/8——GROUP BY 只允许 SELECT 中出现的业务维度列、禁止按维度主键聚合（修复"华东被拆成两行"语义错误）。
  - `data-agent/pyproject.toml`：dev 依赖新增 pytest，加 [tool.pytest.ini_options] pythonpath=["."] testpaths=["tests"]。
  - `data-agent/uv.lock`：同步 pytest 依赖。
  - 新增 `data-agent/tests/test_meta_config.py`（11 个用例，验证四个核心指标、无 GMV/AOV、物理表字段不变、产品/客户等级文案）。
- 元数据重建：先执行等价 meta.sql 的 DDL 重建 meta2 四张表（构建脚本不清理 MySQL 元数据，会报 Duplicate entry），再跑 build_meta_knowledge。重建后 meta2：5 表/24 字段/4 指标/5 关系；Qdrant column 104 points、metric 23 points；ES data-agent-value_index 75 docs（索引名硬编码在 value_es_repository.py，不是 app_config 的 index_name）。
- 五问验收（SSE 均返回 result 且与独立 SQL 复算一致）：①2025各月销售额 109030.5/80009.0/90120.0（验收值精确一致）②各区域销售额排名 华东107373.0/华南70202.0/华北41099.5/西南31528.0/华中28957.0 ③各产品类别销售数量 休闲零食298/食品饮料246/手机数码29/家用电器19/服饰18/鞋靴12 ④2025年2月各产品销售额 15 行与复算一致 ⑤黄金等级客户销售额 100178.5（=6 个黄金客户明细之和）。
- pytest：11 passed。
- 关键教训：①LLM 生成 SQL 按 region_id+region_name 分组会把同一销售区域拆行（dim_region 中一个 region_name 对应多个 region_id），通过 prompt 规则约束 GROUP BY 修复；②build_meta_knowledge 不清理 MySQL meta2 旧数据，重跑前必须重建 meta2 表。

## Stage 2：QueryService 结构化执行边界（完成，基线 ac56a6a）
- 目标：不改变外部 /api/query，让问数 Graph 同时具备 execute()/stream()、QueryExecutionResult、QueryTable、result_contract、稳定 stage_code；未实现 Attribution Graph，未进入 Stage 3。
- 新增：
  - `data-agent/app/models/analysis.py`：JsonScalar、ObservationStatus(str,Enum)、QueryTable（columns/rows/row_count，validator 强制 row_count==len(rows)，Decimal 自动转 float，空结果保留 columns）、QueryExecutionResult（query/sql/table/status/error，状态一致性校验：success 需 sql+row_count>0+error=null；failed 必须有 error）。只放 Stage 2 所需模型，未提前实现归因枚举/模型。
  - `data-agent/tests/test_analysis_models.py`（QueryTable/QueryExecutionResult）、`test_query_service.py`（execute success/empty/failed、stream、search SSE+错误安全、result_contract 进 State/Prompt）、`test_stage_codes.py`（12 节点 stage_code）、`test_dw_repository.py`（execute_query columns/rows/empty 保留 columns）。
- 修改：
  - `app/agent/state.py`：DataAgentState 最小新增 result_contract/result_columns/result_rows（NotRequired），不加归因字段。
  - `app/repositories/mysql/dw_mysql_repository.py`：新增 execute_query(sql) -> (columns, rows)，用 result.keys()+mappings().all()；execute_sql() 保留。
  - `app/agent/nodes/execute_sql.py`：改调 execute_query，返回 result_columns/result_rows 写回 State；custom 事件保留 legacy {"result": rows}，另发内部 {"query_result": {sql,columns,rows}}；异常 logger.error(exc_info=True)。
  - `app/agent/nodes/generate_sql.py` + `prompts/generate_sql.prompt`：读取 state.get("result_contract")，非空时 yaml.dump 进 prompt 的 {result_contract} 占位符（空时传"无（普通问数，不约束输出列名）"）；prompt 新增规则 9/10：非空时严格按契约列别名、period_values 固定值，不得增改输出列；None 时保持普通问数；Stage 1 GROUP BY 规则 7/8 保留。
  - `app/agent/nodes/*.py` 12 个节点：只加 stage_code（extract/recall/merge/filter/add_extra_context → query_retrieval；generate_sql → sql_generation；validate/correct → sql_validation；execute → sql_execution），保留中文 stage，不改业务逻辑。
  - `app/services/query_service.py`：新增 execute(query, result_contract=None) -> QueryExecutionResult（graph.ainvoke 拿最终 state，以 result_columns/result_rows 是否写入判 success/empty/failed）；stream(query) 用 graph.astream(stream_mode="custom")；search() 保留为 SSE 兼容层（内部走 stream），异常输出安全错误信息；详细异常写服务端日志，禁止向用户暴露 str(e)/traceback/路径/凭据。
- pytest：51 passed（基线 11 + 新增 40）。
- 端到端回归（真实 MySQL/Qdrant/ES/LLM）：
  - 五问 execute 全 success：①2025各月销售额 109030.5/80009.0/90120.0（固定值精确一致，独立 SQL 复算 OK）②区域排名 5 行 华东107373.0/华南70202.0/华北41099.5/西南31528.0/华中28957.0（华东未拆分）③产品类别销售数量 6 行 ④2025年2月产品销售额 15 行 ⑤黄金客户销售额 100178.5。
  - execute empty：查 2026 年 → status=empty，columns=['month','sales_amount'] 保留。
  - result_contract：SQL 严格输出 period_key/dimension_value/sales_amount，period_key=comparison/current（UNION ALL 两段），固定 alias 生效。
  - search()：13 条 SSE 事件（stage 带 stage_code + result 保留），legacy /api/query 链路兼容。
  - execute 返回完整 QueryExecutionResult（type/query/sql/table/status/error 均齐）。
- 已知风险：①execute failed 端到端难稳定触发（LLM 通常能生成可执行 SQL），failed 路径由单测覆盖（Graph 异常/无结果列两种）；②LLM 输出的中文列别名有波动（如"月份" vs "month"），数值正确但列名不稳定，result_contract 可缓解归因场景；③verify 脚本中发现固定值匹配需按 '202501' 格式（非 '2025-01'）。
- 未 add/commit/push；docs/决策文档.md 有架构 Agent 的 Stage 1 验收未提交记录（非本次改动）。

## Stage 2 契约小修（架构复核 e30a0743 三项问题修复）
- 架构复核结论：Stage 2 整体符合 SPEC，保留现有实现；需修复三项 Stage 2 范围内问题后才能进 Stage 3。
- 修复 1 legacy SSE 泄露 query_result：`search()` 增加白名单过滤（仅 stage/stage_code/result 可对外），内部 `{"query_result": ...}` 事件不再进入外部 SSE；`stream()` 仍透传全部 custom 事件供后续 AnalysisService 使用。
- 修复 2 QueryTable 值预归一化时机错误：Decimal→float、date→str 的归一化从 QueryTable model validator 移到 `DWMysqlRepository.execute_query` 读取边界；QueryTable 改为 mode="before" 严格校验（非 JsonScalar 直接拒绝，不再静默转换）。
- 修复 3 QueryExecutionResult empty 校验不完整：补全为空 = SQL 成功执行但无数据，要求 sql 非空 + row_count==0 + error=null。
- 测试：51 → 56 passed（新增 非JsonScalar 拒绝、empty 需 sql、empty 需 row_count==0、Decimal 归一化、date 归一化、search 过滤 query_result）。
- 修复后回归（真实链路）：search() 12 条 SSE 事件无 query_result 泄露；execute success 固定值 109030.5/80009.0/90120.0 OK；empty 保留 columns + sql OK；区域排名 5 行华东未拆分 OK；result_contract 三个查询（类别/区域/总体）columns 均为 period_key/dimension_value/sales_amount、period_key=comparison/current，固定 alias 生效 OK。
- 注意：result_contract 靠 LLM 遵守 prompt，存在单次波动（曾输出普通列名），重试即通过；Stage 4 Normalizer 按契约校验、不遵守时 Observation=failed 属设计内防御。

## Stage 3：统一 AnalysisService + 新 SSE 契约 + query 模式前端迁移（完成，commit d67b3db）
- 目标：POST /api/query → AnalysisService → Intent Router → query/attribution 分流；query 模式完整跑通新 SSE 契约 route→stage*→query_result→done；前端从 legacy（data.stage/data.result/Object.keys(rows[0])）迁移到正式事件（data.type/table.columns/table.rows/done）。attribution 只识别 + NOT_IMPLEMENTED 受控失败，绝不伪造归因成功。
- 新增文件（后端）：
  - `app/models/analysis.py` 追加 RequestMode/AnalysisMode/RouteSource/AnalysisStatus/RouteResult（resolved_mode 只允许 query/attribution，validator 兜底禁止 auto；未提前加 Stage 4 对象）。
  - `app/attribution/__init__.py` + `app/attribution/intent_router.py`：优先级 forced > attribution 强规则 > query 强规则 > LLM。attribution 关键词：为什么/原因/归因/哪些因素导致/下降原因/增长原因/增长但/下降但；query 关键词：统计/查询/多少/排名/分别是多少；同时命中 attribution 优先。LLM 二分类 JSON（{"mode":"query"|"attribution"}），prompt 用 replace("{query}") 而非 str.format（prompt 内 JSON 示例花括号会触发 KeyError，踩坑一次）；非法/异常重试一次，二次失败降级 query + warning（source 仍记 llm）。
  - `prompts/attribution_intent.prompt`：只做二分类，禁止输出 SQL/原因分析/推理。
  - `app/services/analysis_service.py`：HTTP 层唯一业务入口。iter_events 产出结构化事件、stream() 序列化为 "data: {json} \n\n"。stage 状态机 _StageTracker：连续同 stage_code 去重、进入新 code 前旧 stage→success、query_result 前当前 stage→success、异常→failed；legacy {"result"} 忽略；内部 query_result → 正式 query_result（QueryTable 校验 row_count，empty 保留 columns，status=success/empty）；错误码按可判断阶段映射 QUERY_GENERATION/VALIDATION/EXECUTION_FAILED，无法判断 INTERNAL_ERROR，message 全部安全文案；done 各路径只发一次；analysis_id=get_req_id()（空则 uuid4）。attribution → route + error(NOT_IMPLEMENTED, fatal) + done(attribution, failed, query_count=0)。
  - `app/api/schemas/query_schema.py`：query trim 后 1~1000、mode 默认 auto（复用 RequestMode 枚举），非法 422 在 SSE 建立前由 Pydantic 返回。
  - `app/api/dependencies.py` get_analysis_service（复用 get_query_service）；`query_router.py` 只 QuerySchema→Depends→StreamingResponse，不做 intent 判断。
- 新增文件（前端）：`src/composables/useAnalysisStream.js`（fetch POST /api/query {query,mode} → reader→TextDecoder→buffer→\n\n 分割→按 data.type 回调；收到 done 即 reader.cancel() 结束 loading；连接关闭未收到 done 抛错；HTTP 非 2xx 抛参数非法）；`src/components/AnalysisTimeline.vue`（stage/status running/success/failed）；`src/components/ResultTable.vue`（列只来自 table.columns/rows，空结果空状态）；`src/App.vue` 重写（页面骨架+输入+消息；route 徽标/时间线/表格/error；upsertStage 按 stage_code 更新状态）。
- 测试：新增 test_query_schema(10)/test_intent_router(19)/test_analysis_service(20)，56 → 105 passed，全部 mock 不调真实 LLM/DB。
- 真实回归（云 MySQL/Qdrant/ES + 本地 embedding/LLM）：五问全通过（route(query)→stage*→query_result→done(completed)）；固定值 2025-01/02/03=109030.5/80009.0/90120.0；区域排名 5 行、类别数量 6 行、2025-02 产品 15 行、黄金客户 1 行；外部 SSE 无 legacy result、无内部 query_result wrapper；forced query 覆盖归因文本、forced attribution 也走 NOT_IMPLEMENTED；422 对空 query/超长/mode 非法/缺 query 均生效。npm run build 通过；vite dev 代理 5173→8000 SSE 联调 OK。
- 已知风险：①前端浏览器 UI 交互（时间线动画/表格渲染/错误展示）未经真实浏览器人工验证，代码路径已由 build+联调保证，建议用户浏览器抽查；②LLM 自由列别名波动仍在（非本 Stage 范围，Stage 4 Normalizer 兜底）；③NOT_IMPLEMENTED 为 Stage 3 过渡码，Stage 5 接入真实归因后删除；④stage 状态机依赖 Graph custom 事件顺序，若未来节点发事件顺序变化需复核。
- 未 push（用户偏好：push 由用户操作）。

## Stage 4：归因确定性核心（完成，commit 773ac5f，未 push）
- 目标：只建立不依赖 LLM Planner 的确定性归因核心（Action Router → sub_query+result_contract → QueryExecutionResult → Normalizer → Observation → Calculator → Calculation → Evidence Builder → Evidence），完成 AttributionState/Context 与停止条件纯函数；不要求真实归因链路跑起来；禁止进入 Stage 5。
- 新增（后端）：
  - `app/models/analysis.py` 补齐 Stage 4 冻结对象：ActionType(8)/MetricKey(4)/DimensionKey(5)/CalculationType(3)/FactorDirection(3)/FilterOperator(eq|in_)/Period(start<=end)/FilterCondition(extra=forbid 拒绝 where_sql 等任意 SQL 字段、values≥1)/MetricPeriodValue/ComparisonRow/AttributionTarget(metrics 1~3)/Action(8 类逐类条件校验：compare_period 禁 dimension、analyze_unit_price 必须含 sales_amount+sales_quantity、calculate_contribution metrics 恰好 1 个且 source≥1、finish 不查询；reason 1~200 字)/Observation(success 需 success query_result+归一化行、empty 无行、failed 必带 error)/PeriodChangeCalculation/ContributionItem/ContributionCalculation/UnitPriceCalculation/Evidence(observation_ids≥1，无 confidence 字段)。
  - `app/attribution/state.py`：AttributionState/AttributionContext TypedDict（context 只含 query_service，禁止 Repository/Session/LLM）+ initial_state（max_query_actions=6 初始化约束，report 保持 None 占位）。
  - `app/attribution/action_router.py`：6 查询/2 本地 Action 判断；冻结逻辑键去重 (type, 规范化 metrics, 两期间, dimension, 规范化 filters，in values 排序)；query_action_count≥6 拒绝；sub_query 确定性模板（不拼 SQL）；内部 result_contract（period_alias=period_key/period_values=[comparison,current]/dimension_alias/dimension_value）；停止纯函数（is_force_stopped=count≥6 或连续 empty/failed≥2；forced_status=有有效 Evidence→partial 否则 failed，达上限不自动 completed；next_consecutive=success 归 0；can_finish=总体比较+≥2 维度拆解+driver 证据+非强制停止）；ActionRouter.validate 状态化校验（ACTION_LIMIT_REACHED/FORCED_STOP/ACTION_DUPLICATE/INVALID_FILTER/PREMATURE_FINISH/INVALID_CONTRIBUTION_SOURCE/CONTRIBUTION_ALREADY_CALCULATED）。
  - `app/attribution/normalizer.py`：严格按 contract 归一化，不猜列；period_key 只允许 comparison/current；成员单期间出现→另一期间补 0（非未知）；empty→normalized_rows=[]；provider failed→failed 原样保留 QueryExecutionResult；契约不匹配→RESULT_NORMALIZATION_FAILED 安全语义；(member,period) 重复行也判 failed。
  - `app/attribution/calculator.py`：纯 Decimal(str(v))+ROUND_HALF_UP，金额/单价 2 位小数、比率 4 位小数输出 float；period_change（comparison=0→rate None）、contribution（total_delta=0→全部 rate None+neutral；同号 driver/反号 offset/0 或总 0 neutral；rate 允许 >1/<0 不 clamp）、unit_price（quantity=0→单价 None→delta/change_rate 置 None 禁止补值）。
  - `app/attribution/evidence_builder.py`：只接受 success Observation；period_change/contribution(每 member 一条)/unit_price 三类证据；计算型证据强制引用对应 Calculation（类型+source 校验）；不复制 SQL（追溯链 Evidence→observation_id→query_result.sql）；statement 只含来源数值，无 confidence。
  - `tests/attribution/`：__init__.py + test_action_router(38)/test_normalizer(18)/test_calculator(15)/test_evidence_builder(13)/test_stopping(18)；`tests/test_analysis_models.py` 追加 28 个 Stage 4 模型用例。
- 固定数值复算全部通过：场景一 109030.5→80009.0，delta=-29021.5，rate≈-0.2662；场景二 数量 151→322 rate≈1.1325、金额 80009→90120 rate≈0.1264、平均单件 529.86→279.88。
- 测试：Stage 4 新增 148 个用例 + 全量回归 241 passed（含 Stage 2/3 既有 91+2 个，无退化）；compileall app 通过。
- 关键实现决策：①Action/FilterCondition 用 extra="forbid" 从字段层根绝 where_sql/SQL 表达式（pydantic v2 默认忽略 extra 字段，必须显式 forbid）；②breakdown 各类型必须校验 dimension 等于对应枚举值（最初只查非空导致 category 能冒充 region）；③Normalizer 对 (member,period) 重复行判 failed（契约歧义不猜测）；④无 stopping.py——停止/finish 判定全部作为 action_router.py 纯函数实现。
- 验收核查：无 Stage 5 文件（target_parser/planner/graph/report_generator/prompts 未创建）；未改 AnalysisService（route(attribution)→NOT_IMPLEMENTED→done(failed) 保留）；未改前端；未新增基础设施/持久化表。
- commit：773ac5f（仅含 13 个 Stage 4 文件；docs/决策文档.md 与 .workbuddy 记忆文件为既有未提交改动，未纳入）；未 push（用户偏好）。
- 已知风险：①finish 判定依赖 observations/evidences 快照，Stage 5 接 Planner 时需把 consecutive_empty_or_failed 传入 router.validate（签名已留默认参数）；②Normalizer 对字符串数值严格拒绝（只收 int/float），若 SQL 引擎返回 varchar 数值列会 failed，属设计内防御；③贡献率/变化率 4 位小数四舍五入为 ROUND_HALF_UP，前端展示百分比时自行乘 100。


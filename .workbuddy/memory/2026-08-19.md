# 2026-08-19 工作日志

## Stage 6：归因分析工作台前端

- **目标**：基于已验收的 Stage 5 后端，实现第一版前端归因展示；完成后停止，不进入 Stage 7。
- **基线**：`ce22984c563b77448965e347b694727c66e87251`（HEAD 的祖先，已确认）。
- **新增文件**：
  - `date-agent-frontend/src/components/AttributionReport.vue`：七段报告（问题定义/核心结论/指标概览/驱动/抵消/Evidence 明细/数据边界与建议）+「复制报告」(Clipboard API, 中文纯文本) + Evidence→observation→SQL/QueryTable 追溯（找不到显示“查询明细不可用”）+ partial/failed 处理。
  - `date-agent-frontend/src/components/ContributionChart.vue`：原生 Vue+CSS 条形图，仅用 `report.drivers`/`report.offsets`，长度按 `abs(delta)` 归一化，不按 contribution_rate，无图表库。
- **修改文件**：
  - `date-agent-frontend/src/App.vue`：每条 assistant message 维护完整 attribution 状态（mode/stages/actions/queryResults/calculations/evidences/report/table/done/error/loading）；按 `data.type` 分发；普通 query 继续写 `table`，attribution `query_result` 追加 `queryResults[]` 不覆盖；新增 3 个示例问题（点击直接发送）；组合子组件，报告七段渲染不在 App 内。
  - `date-agent-frontend/src/components/AnalysisTimeline.vue`：从只展示 stage 扩展为 stage + Action 卡片 + 每次查询执行结果（sub_query/success·empty·failed/SQL 折叠/QueryTable）。
  - `useAnalysisStream.js`：确认已正确透传全部 Stage 6 事件（done 为结束依据、未收 done 报连接异常），无需修改。
- **验证**：
  - `npm run build` 通过（21 modules，无错误）。
  - 用 Node 复刻事件→状态归约，喂入冻结场景事件序列，22/22 断言通过（attribution 追加不覆盖、evidence 去重、report 七段、追溯解析、failed 无报告、partial 有报告、普通 query 走 table）。
  - **未做真实端到端**：本沙箱到云端 MySQL/Qdrant/ES（49.235.159.92:3308/6333/9200）不可达，Stage 5 后端依赖它们+LLM，无法在此启动；三个场景的浏览器人工验证需由用户在可连通云端的 Mac 上完成。
- **提交**：独立 commit `02e5fca`（仅前端 4 文件，未 push）。

## Stage 6 修复：报告复制结构与多 Observation 追溯

- **基线**：`02e5fca98f0ab35ddba4adf1cf4a5e7040402340`（上轮提交）。
- **范围**：仅改 `date-agent-frontend/src/components/AttributionReport.vue`（未动后端/API/SSE/其他 Stage 6 文件）。
- **三处修复**：
  1. 复制换行：基线已用 `lines.join("\n")`，保持并验证（多行正常）。
  2. 复制七段结构：基线误写成「六、数据边界 / 七、建议 / 八、证据明细」。改为冻结结构「六、证据明细 / 七、数据边界与建议」（第七部分内部分别含「数据边界：」「建议：」），删除「八、证据明细」，保留 Evidence ID/Observation ID/Calculation ID。
  3. 多 Observation 追溯：删除 `hasTrace()`（原用 `.some` 任一存在即整体可追）。模板改为对每个 `observation_id` 独立判断：存在→展示 SQL/QueryTable，缺失→该 observation 下显示「查询明细不可用」；空 observation_ids 时也显示「查询明细不可用」。
- **验证**：`npm run build` 通过（21 modules）；从实际文件抽取 `buildReportText` 并执行 + 逐 observation 追溯断言，16/16 通过（含：仅 7 个一级章节、第六=证据明细、第七=数据边界与建议、无八、多行换行、E1/E2/o1/o2/o3/calc-1 均保留、o1 可追/o2 不可用）。
- **真实端到端**：本沙箱到云端 MySQL/Qdrant/ES 仍不可达（3308/6333/9200 均 CLOSED），无法启动后端跑两个冻结归因场景；需用户在可连通云端的 Mac 上验证。
- **提交**：独立 commit `3ba936b`（仅 AttributionReport.vue，未 push）。

## 已知 UI 风险
- 当前为单次会话页面，刷新即丢失状态（符合第一版设计，无持久化）。
- 数字格式化用 `String(Number(v))`，`80009.0` 会显示为 `80009`（同值），如需严格保留 `.0` 需后端改序列化或前端补格式。
- 归因消息气泡在窄屏会横向滚动（表格 overflow-x），属可接受范围。

## Stage 6 最终浏览器验收（真实前后端 + 真实浏览器）

- **结论**：三条真实链路全部通过，未修改功能代码，未进入 Stage 7，停止等待架构复核。
- **启动**：后端 `uv run python main.py`（:8000，进程内 bge 模型，无报错）；前端 `npm run dev`（:5173，Vite 代理→:8000）。`npm ci` 被 WorkBuddy safe-delete 守卫拦截，改用 `npm install --yes` 修复缺失的 `.bin` 符号链接（非破坏性，未改源码/lockfile）。
- **场景一**（为什么2025年2月销售额较1月明显下降？）：PASS。mode=经营归因，时间线/Action/多次Query不覆盖/SQL/QueryTable/数值(109030.5/80009.0/-29021.5/-26.62%)/2+有效拆解(产品+客户)/drivers+offsets/ContributionChart/Evidence→Observation→SQL 追溯/数据边界+建议/复制报告，控制台 0 异常、无敏感词。
- **场景二**（3月数量大增销售额有限）：PASS。数量151/322/+113.25%，销售额80009.0/90120.0/+12.64%，平均单件529.86→279.88（后端 Calculation），追溯链完整，0 异常。
- **普通问数回归**（统计2025年各月销售额）：PASS。route=普通问数，ResultTable 正常，不进 AttributionReport，done 后结束 loading，0 异常。
- **关键发现（交架构复核）**：Stage 6 页面渲染无缺陷；但 2月vs1月 的 breakdown 在**后端归因层不稳定**——一次运行 region/category 拆解被 LLM 标记为“未取得有效数据”（无驱动因素），另一次及场景二正常产出有效拆解。属 Stage 5 非确定性，页面两种结果均正确渲染。AGENTS.md 要求的“确定性期望结果”尚未满足。
- **测试脚手架坑（非应用 bug）**：无头 Chrome 对 >数分钟的长 SSE 在空闲时会挂起 URLLoader（`net::ERR_NETWORK_IO_SUSPENDED`）。需用 Playwright 反后台化参数（`--disable-backgrounding-occluded-windows --disable-renderer-backgrounding --disable-background-timer-throttling --disable-features=CalculateNativeWinOcclusion,IntensiveWakeUpThrottling`）+ 两次重归因之间留后端冷却；真实前台浏览器不受影响。验收脚本：`accept.cjs`、`debug_s1.cjs`。
- **产物**：`docs/stage6_browser_acceptance.md`（验收报告）。
